{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build run_tests",
            "type": "shell",
            "command": "cmake --build cmake-build-debug",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [
                "$gcc"
            ]
        }
    ]
}
